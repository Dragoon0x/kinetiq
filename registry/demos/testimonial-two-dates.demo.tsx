"use client";

import { TestimonialTwoDates } from "@/registry/blocks/testimonial-two-dates/testimonial-two-dates";

/** The section at its own scale — full width, default narrative. */
export function TestimonialTwoDatesDemo() {
  return (
    <div className="w-full">
      <TestimonialTwoDates />
    </div>
  );
}
