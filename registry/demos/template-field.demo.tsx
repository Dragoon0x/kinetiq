"use client";

import { TemplateField } from "@/registry/templates/template-field/template-field";

/** The whole site, at its own scale. */
export function TemplateFieldDemo() {
  return <TemplateField />;
}
