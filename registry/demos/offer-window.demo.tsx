"use client";

import { OfferWindow } from "@/registry/blocks/offer-window/offer-window";

/** The section at its own scale — full width, default narrative. */
export function OfferWindowDemo() {
  return (
    <div className="w-full">
      <OfferWindow />
    </div>
  );
}
