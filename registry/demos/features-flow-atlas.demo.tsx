"use client";

import { FeaturesFlowAtlas } from "@/registry/blocks/features-flow-atlas/features-flow-atlas";

/** The section at its own scale — full width, default narrative. */
export function FeaturesFlowAtlasDemo() {
  return (
    <div className="w-full">
      <FeaturesFlowAtlas />
    </div>
  );
}
