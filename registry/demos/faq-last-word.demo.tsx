"use client";

import { FaqLastWord } from "@/registry/blocks/faq-last-word/faq-last-word";

/** The section at its own scale — full width, default narrative. */
export function FaqLastWordDemo() {
  return (
    <div className="w-full">
      <FaqLastWord />
    </div>
  );
}
