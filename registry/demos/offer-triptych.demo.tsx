"use client";

import { OfferTriptych } from "@/registry/blocks/offer-triptych/offer-triptych";

/** The section at its own scale — full width, default narrative. */
export function OfferTriptychDemo() {
  return (
    <div className="w-full">
      <OfferTriptych />
    </div>
  );
}
