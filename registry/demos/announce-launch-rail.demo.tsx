"use client";

import { AnnounceLaunchRail } from "@/registry/blocks/announce-launch-rail/announce-launch-rail";

/** The section at its own scale — full width, default narrative. */
export function AnnounceLaunchRailDemo() {
  return (
    <div className="w-full">
      <AnnounceLaunchRail />
    </div>
  );
}
