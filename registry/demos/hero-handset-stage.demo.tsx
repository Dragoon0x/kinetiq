"use client";

import { HeroHandsetStage } from "@/registry/blocks/hero-handset-stage/hero-handset-stage";

/** The section at its own scale — full width, default narrative. */
export function HeroHandsetStageDemo() {
  return (
    <div className="w-full">
      <HeroHandsetStage />
    </div>
  );
}
