"use client";

import { TrustDataResidency } from "@/registry/blocks/trust-data-residency/trust-data-residency";

/** The section at its own scale — full width, default narrative. */
export function TrustDataResidencyDemo() {
  return (
    <div className="w-full">
      <TrustDataResidency />
    </div>
  );
}
