"use client";

import { ErrorMaintenance } from "@/registry/pages/error-maintenance/error-maintenance";

/** The whole page, at its own scale. */
export function ErrorMaintenanceDemo() {
  return <ErrorMaintenance />;
}
