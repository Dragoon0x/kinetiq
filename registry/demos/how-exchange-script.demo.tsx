"use client";

import { HowExchangeScript } from "@/registry/blocks/how-exchange-script/how-exchange-script";

/** The section at its own scale — full width, default narrative. */
export function HowExchangeScriptDemo() {
  return (
    <div className="w-full">
      <HowExchangeScript />
    </div>
  );
}
