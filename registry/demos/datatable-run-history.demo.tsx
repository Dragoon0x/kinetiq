"use client";

import { DatatableRunHistory } from "@/registry/blocks/datatable-run-history/datatable-run-history";

/** The section at its own scale — full width, default narrative. */
export function DatatableRunHistoryDemo() {
  return (
    <div className="w-full">
      <DatatableRunHistory />
    </div>
  );
}
