"use client";

import { AnnounceScheduledWindow } from "@/registry/blocks/announce-scheduled-window/announce-scheduled-window";

/** The section at its own scale — full width, default narrative. */
export function AnnounceScheduledWindowDemo() {
  return (
    <div className="w-full">
      <AnnounceScheduledWindow />
    </div>
  );
}
