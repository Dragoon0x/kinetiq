"use client";

import { TemplateLaunch } from "@/registry/templates/template-launch/template-launch";

/** The whole site, at its own scale. */
export function TemplateLaunchDemo() {
  return <TemplateLaunch />;
}
