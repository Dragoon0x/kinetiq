"use client";

import { ContactOpenHours } from "@/registry/blocks/contact-open-hours/contact-open-hours";

/** The section at its own scale — full width, default narrative. */
export function ContactOpenHoursDemo() {
  return (
    <div className="w-full">
      <ContactOpenHours />
    </div>
  );
}
