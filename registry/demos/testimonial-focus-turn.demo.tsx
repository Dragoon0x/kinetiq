"use client";

import { TestimonialFocusTurn } from "@/registry/blocks/testimonial-focus-turn/testimonial-focus-turn";

/** The section at its own scale — full width, default narrative. */
export function TestimonialFocusTurnDemo() {
  return (
    <div className="w-full">
      <TestimonialFocusTurn />
    </div>
  );
}
