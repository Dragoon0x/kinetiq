"use client";

import { ContactDirectLines } from "@/registry/blocks/contact-direct-lines/contact-direct-lines";

/** The section at its own scale — full width, default narrative. */
export function ContactDirectLinesDemo() {
  return (
    <div className="w-full">
      <ContactDirectLines />
    </div>
  );
}
