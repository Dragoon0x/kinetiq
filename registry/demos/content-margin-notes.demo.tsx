"use client";

import { ContentMarginNotes } from "@/registry/blocks/content-margin-notes/content-margin-notes";

/** The section at its own scale — full width, default narrative. */
export function ContentMarginNotesDemo() {
  return (
    <div className="w-full">
      <ContentMarginNotes />
    </div>
  );
}
