"use client";

import { FeaturesRelayTabs } from "@/registry/blocks/features-relay-tabs/features-relay-tabs";

/** The section at its own scale — full width, default narrative. */
export function FeaturesRelayTabsDemo() {
  return (
    <div className="w-full">
      <FeaturesRelayTabs />
    </div>
  );
}
