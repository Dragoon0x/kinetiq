"use client";

import { HeroConsoleDrift } from "@/registry/blocks/hero-console-drift/hero-console-drift";

/** The section at its own scale — full width, default narrative. */
export function HeroConsoleDriftDemo() {
  return (
    <div className="w-full">
      <HeroConsoleDrift />
    </div>
  );
}
