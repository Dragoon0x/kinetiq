"use client";

import { TestimonialStandingDesk } from "@/registry/blocks/testimonial-standing-desk/testimonial-standing-desk";

/** The section at its own scale — full width, default narrative. */
export function TestimonialStandingDeskDemo() {
  return (
    <div className="w-full">
      <TestimonialStandingDesk />
    </div>
  );
}
