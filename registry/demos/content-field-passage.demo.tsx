"use client";

import { ContentFieldPassage } from "@/registry/blocks/content-field-passage/content-field-passage";

/** The section at its own scale — full width, default narrative. */
export function ContentFieldPassageDemo() {
  return (
    <div className="w-full">
      <ContentFieldPassage />
    </div>
  );
}
