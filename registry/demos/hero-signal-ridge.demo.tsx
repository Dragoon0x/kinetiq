"use client";

import { HeroSignalRidge } from "@/registry/blocks/hero-signal-ridge/hero-signal-ridge";

/** The section at its own scale — full width, default narrative. */
export function HeroSignalRidgeDemo() {
  return (
    <div className="w-full">
      <HeroSignalRidge />
    </div>
  );
}
