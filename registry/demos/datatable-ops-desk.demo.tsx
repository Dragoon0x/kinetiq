"use client";

import { DatatableOpsDesk } from "@/registry/blocks/datatable-ops-desk/datatable-ops-desk";

/** The section at its own scale — full width, default narrative. */
export function DatatableOpsDeskDemo() {
  return (
    <div className="w-full">
      <DatatableOpsDesk />
    </div>
  );
}
