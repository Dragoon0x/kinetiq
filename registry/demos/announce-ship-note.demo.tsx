"use client";

import { AnnounceShipNote } from "@/registry/blocks/announce-ship-note/announce-ship-note";

/** The section at its own scale — full width, default narrative. */
export function AnnounceShipNoteDemo() {
  return (
    <div className="w-full">
      <AnnounceShipNote />
    </div>
  );
}
