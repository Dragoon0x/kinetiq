"use client";

import { TeamFocusPanels } from "@/registry/blocks/team-focus-panels/team-focus-panels";

/** The section at its own scale — full width, default narrative. */
export function TeamFocusPanelsDemo() {
  return (
    <div className="w-full">
      <TeamFocusPanels />
    </div>
  );
}
