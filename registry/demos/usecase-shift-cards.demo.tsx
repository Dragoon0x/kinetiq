"use client";

import { UsecaseShiftCards } from "@/registry/blocks/usecase-shift-cards/usecase-shift-cards";

/** The section at its own scale — full width, default narrative. */
export function UsecaseShiftCardsDemo() {
  return (
    <div className="w-full">
      <UsecaseShiftCards />
    </div>
  );
}
