"use client";

import { StatsRingSet } from "@/registry/blocks/stats-ring-set/stats-ring-set";

/** The section at its own scale — full width, default narrative. */
export function StatsRingSetDemo() {
  return (
    <div className="w-full">
      <StatsRingSet />
    </div>
  );
}
