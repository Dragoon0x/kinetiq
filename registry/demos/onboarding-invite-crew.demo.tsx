"use client";

import { OnboardingInviteCrew } from "@/registry/pages/onboarding-invite-crew/onboarding-invite-crew";

/** The whole page, at its own scale. */
export function OnboardingInviteCrewDemo() {
  return <OnboardingInviteCrew />;
}
