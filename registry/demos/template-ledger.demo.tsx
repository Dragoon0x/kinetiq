"use client";

import { TemplateLedger } from "@/registry/templates/template-ledger/template-ledger";

/** The whole site, at its own scale. */
export function TemplateLedgerDemo() {
  return <TemplateLedger />;
}
