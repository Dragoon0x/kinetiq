"use client";

import { TrustVaultBrief } from "@/registry/blocks/trust-vault-brief/trust-vault-brief";

/** The section at its own scale — full width, default narrative. */
export function TrustVaultBriefDemo() {
  return (
    <div className="w-full">
      <TrustVaultBrief />
    </div>
  );
}
