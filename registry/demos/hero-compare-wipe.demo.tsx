"use client";

import { HeroCompareWipe } from "@/registry/blocks/hero-compare-wipe/hero-compare-wipe";

/** The section at its own scale — full width, default narrative. */
export function HeroCompareWipeDemo() {
  return (
    <div className="w-full">
      <HeroCompareWipe />
    </div>
  );
}
