"use client";

import { OnboardingFirstRun } from "@/registry/pages/onboarding-first-run/onboarding-first-run";

/** The whole page, at its own scale. */
export function OnboardingFirstRunDemo() {
  return <OnboardingFirstRun />;
}
