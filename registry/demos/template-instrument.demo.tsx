"use client";

import { TemplateInstrument } from "@/registry/templates/template-instrument/template-instrument";

/** The whole site, at its own scale. */
export function TemplateInstrumentDemo() {
  return <TemplateInstrument />;
}
