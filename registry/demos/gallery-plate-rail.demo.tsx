"use client";

import { GalleryPlateRail } from "@/registry/blocks/gallery-plate-rail/gallery-plate-rail";

/** The section at its own scale — full width, default narrative. */
export function GalleryPlateRailDemo() {
  return (
    <div className="w-full">
      <GalleryPlateRail />
    </div>
  );
}
