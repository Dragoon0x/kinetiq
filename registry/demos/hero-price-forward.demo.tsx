"use client";

import { HeroPriceForward } from "@/registry/blocks/hero-price-forward/hero-price-forward";

/** The section at its own scale — full width, default narrative. */
export function HeroPriceForwardDemo() {
  return (
    <div className="w-full">
      <HeroPriceForward />
    </div>
  );
}
