"use client";

import { FeaturesPersonaSwitch } from "@/registry/blocks/features-persona-switch/features-persona-switch";

/** The section at its own scale — full width, default narrative. */
export function FeaturesPersonaSwitchDemo() {
  return (
    <div className="w-full">
      <FeaturesPersonaSwitch />
    </div>
  );
}
