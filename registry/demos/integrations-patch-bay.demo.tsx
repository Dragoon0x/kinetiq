"use client";

import { IntegrationsPatchBay } from "@/registry/blocks/integrations-patch-bay/integrations-patch-bay";

/** The section at its own scale — full width, default narrative. */
export function IntegrationsPatchBayDemo() {
  return (
    <div className="w-full">
      <IntegrationsPatchBay />
    </div>
  );
}
