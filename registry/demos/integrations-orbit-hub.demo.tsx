"use client";

import { IntegrationsOrbitHub } from "@/registry/blocks/integrations-orbit-hub/integrations-orbit-hub";

/** The section at its own scale — full width, default narrative. */
export function IntegrationsOrbitHubDemo() {
  return (
    <div className="w-full">
      <IntegrationsOrbitHub />
    </div>
  );
}
