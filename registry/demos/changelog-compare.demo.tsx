"use client";

import { ChangelogCompare } from "@/registry/pages/changelog-compare/changelog-compare";

/** The whole page, at its own scale. */
export function ChangelogCompareDemo() {
  return <ChangelogCompare />;
}
