"use client";

import { ChangelogRelease } from "@/registry/pages/changelog-release/changelog-release";

/** The whole page, at its own scale. */
export function ChangelogReleaseDemo() {
  return <ChangelogRelease />;
}
