"use client";

import { NewsletterBackIssues } from "@/registry/blocks/newsletter-back-issues/newsletter-back-issues";

/** The section at its own scale — full width, default narrative. */
export function NewsletterBackIssuesDemo() {
  return (
    <div className="w-full">
      <NewsletterBackIssues />
    </div>
  );
}
