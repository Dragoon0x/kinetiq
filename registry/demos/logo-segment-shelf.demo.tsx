"use client";

import { LogoSegmentShelf } from "@/registry/blocks/logo-segment-shelf/logo-segment-shelf";

/** The section at its own scale — full width, default narrative. */
export function LogoSegmentShelfDemo() {
  return (
    <div className="w-full">
      <LogoSegmentShelf />
    </div>
  );
}
