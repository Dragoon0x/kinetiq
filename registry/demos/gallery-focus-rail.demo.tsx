"use client";

import { GalleryFocusRail } from "@/registry/blocks/gallery-focus-rail/gallery-focus-rail";

/** The section at its own scale — full width, default narrative. */
export function GalleryFocusRailDemo() {
  return (
    <div className="w-full">
      <GalleryFocusRail />
    </div>
  );
}
