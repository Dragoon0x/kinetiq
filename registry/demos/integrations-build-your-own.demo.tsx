"use client";

import { IntegrationsBuildYourOwn } from "@/registry/blocks/integrations-build-your-own/integrations-build-your-own";

/** The section at its own scale — full width, default narrative. */
export function IntegrationsBuildYourOwnDemo() {
  return (
    <div className="w-full">
      <IntegrationsBuildYourOwn />
    </div>
  );
}
