"use client";

import { HowPlainSteps } from "@/registry/blocks/how-plain-steps/how-plain-steps";

/** The section at its own scale — full width, default narrative. */
export function HowPlainStepsDemo() {
  return (
    <div className="w-full">
      <HowPlainSteps />
    </div>
  );
}
