"use client";

import { FeaturesQuietGrid } from "@/registry/blocks/features-quiet-grid/features-quiet-grid";

/** The section at its own scale — full width, default narrative. */
export function FeaturesQuietGridDemo() {
  return (
    <div className="w-full">
      <FeaturesQuietGrid />
    </div>
  );
}
