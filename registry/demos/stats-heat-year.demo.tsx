"use client";

import { StatsHeatYear } from "@/registry/blocks/stats-heat-year/stats-heat-year";

/** The section at its own scale — full width, default narrative. */
export function StatsHeatYearDemo() {
  return (
    <div className="w-full">
      <StatsHeatYear />
    </div>
  );
}
