"use client";

import { HeroQuietWord } from "@/registry/blocks/hero-quiet-word/hero-quiet-word";

/** The section at its own scale — full width, default narrative. */
export function HeroQuietWordDemo() {
  return (
    <div className="w-full">
      <HeroQuietWord />
    </div>
  );
}
