"use client";

import { TestimonialCaseColumn } from "@/registry/blocks/testimonial-case-column/testimonial-case-column";

/** The section at its own scale — full width, default narrative. */
export function TestimonialCaseColumnDemo() {
  return (
    <div className="w-full">
      <TestimonialCaseColumn />
    </div>
  );
}
