"use client";

import { TemplateSignature } from "@/registry/templates/template-signature/template-signature";

/** The whole site, at its own scale. */
export function TemplateSignatureDemo() {
  return <TemplateSignature />;
}
