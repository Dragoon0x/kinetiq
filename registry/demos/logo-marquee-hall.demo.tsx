"use client";

import { LogoMarqueeHall } from "@/registry/blocks/logo-marquee-hall/logo-marquee-hall";

/** The section at its own scale — full width, default narrative. */
export function LogoMarqueeHallDemo() {
  return (
    <div className="w-full">
      <LogoMarqueeHall />
    </div>
  );
}
