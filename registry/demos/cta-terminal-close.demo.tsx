"use client";

import { CtaTerminalClose } from "@/registry/blocks/cta-terminal-close/cta-terminal-close";

/** The section at its own scale — full width, default narrative. */
export function CtaTerminalCloseDemo() {
  return (
    <div className="w-full">
      <CtaTerminalClose />
    </div>
  );
}
