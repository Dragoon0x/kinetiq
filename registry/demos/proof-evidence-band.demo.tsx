"use client";

import { ProofEvidenceBand } from "@/registry/blocks/proof-evidence-band/proof-evidence-band";

/** The section at its own scale — full width, default narrative. */
export function ProofEvidenceBandDemo() {
  return (
    <div className="w-full">
      <ProofEvidenceBand />
    </div>
  );
}
