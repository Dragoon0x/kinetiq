"use client";

import { CareersIndex } from "@/registry/pages/careers-index/careers-index";

/** The whole page, at its own scale. */
export function CareersIndexDemo() {
  return <CareersIndex />;
}
