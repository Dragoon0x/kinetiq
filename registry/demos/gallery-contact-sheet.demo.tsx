"use client";

import { GalleryContactSheet } from "@/registry/blocks/gallery-contact-sheet/gallery-contact-sheet";

/** The section at its own scale — full width, default narrative. */
export function GalleryContactSheetDemo() {
  return (
    <div className="w-full">
      <GalleryContactSheet />
    </div>
  );
}
