"use client";

import { TeamOpenBench } from "@/registry/blocks/team-open-bench/team-open-bench";

/** The section at its own scale — full width, default narrative. */
export function TeamOpenBenchDemo() {
  return (
    <div className="w-full">
      <TeamOpenBench />
    </div>
  );
}
