"use client";

import { UsecaseNotForYou } from "@/registry/blocks/usecase-not-for-you/usecase-not-for-you";

/** The section at its own scale — full width, default narrative. */
export function UsecaseNotForYouDemo() {
  return (
    <div className="w-full">
      <UsecaseNotForYou />
    </div>
  );
}
