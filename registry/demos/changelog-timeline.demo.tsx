"use client";

import { ChangelogTimeline } from "@/registry/pages/changelog-timeline/changelog-timeline";

/** The whole page, at its own scale. */
export function ChangelogTimelineDemo() {
  return <ChangelogTimeline />;
}
