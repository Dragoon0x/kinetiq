"use client";

import { FaqRibbonTabs } from "@/registry/blocks/faq-ribbon-tabs/faq-ribbon-tabs";

/** The section at its own scale — full width, default narrative. */
export function FaqRibbonTabsDemo() {
  return (
    <div className="w-full">
      <FaqRibbonTabs />
    </div>
  );
}
