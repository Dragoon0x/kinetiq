"use client";

import { TemplateStudio } from "@/registry/templates/template-studio/template-studio";

/** The whole site, at its own scale. */
export function TemplateStudioDemo() {
  return <TemplateStudio />;
}
