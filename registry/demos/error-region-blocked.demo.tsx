"use client";

import { ErrorRegionBlocked } from "@/registry/pages/error-region-blocked/error-region-blocked";

/** The whole page, at its own scale. */
export function ErrorRegionBlockedDemo() {
  return <ErrorRegionBlocked />;
}
