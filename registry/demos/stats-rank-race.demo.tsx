"use client";

import { StatsRankRace } from "@/registry/blocks/stats-rank-race/stats-rank-race";

/** The section at its own scale — full width, default narrative. */
export function StatsRankRaceDemo() {
  return (
    <div className="w-full">
      <StatsRankRace />
    </div>
  );
}
