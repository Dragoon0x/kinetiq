"use client";

import { ContactRoutingDesk } from "@/registry/blocks/contact-routing-desk/contact-routing-desk";

/** The section at its own scale — full width, default narrative. */
export function ContactRoutingDeskDemo() {
  return (
    <div className="w-full">
      <ContactRoutingDesk />
    </div>
  );
}
