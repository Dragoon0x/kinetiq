"use client";

import { ErrorOffline } from "@/registry/pages/error-offline/error-offline";

/** The whole page, at its own scale. */
export function ErrorOfflineDemo() {
  return <ErrorOffline />;
}
