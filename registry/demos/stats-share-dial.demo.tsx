"use client";

import { StatsShareDial } from "@/registry/blocks/stats-share-dial/stats-share-dial";

/** The section at its own scale — full width, default narrative. */
export function StatsShareDialDemo() {
  return (
    <div className="w-full">
      <StatsShareDial />
    </div>
  );
}
