"use client";

import { GalleryMosaicMorph } from "@/registry/blocks/gallery-mosaic-morph/gallery-mosaic-morph";

/** The section at its own scale — full width, default narrative. */
export function GalleryMosaicMorphDemo() {
  return (
    <div className="w-full">
      <GalleryMosaicMorph />
    </div>
  );
}
