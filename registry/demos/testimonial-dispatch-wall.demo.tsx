"use client";

import { TestimonialDispatchWall } from "@/registry/blocks/testimonial-dispatch-wall/testimonial-dispatch-wall";

/** The section at its own scale — full width, default narrative. */
export function TestimonialDispatchWallDemo() {
  return (
    <div className="w-full">
      <TestimonialDispatchWall />
    </div>
  );
}
