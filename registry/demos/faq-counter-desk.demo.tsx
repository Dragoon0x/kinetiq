"use client";

import { FaqCounterDesk } from "@/registry/blocks/faq-counter-desk/faq-counter-desk";

/** The section at its own scale — full width, default narrative. */
export function FaqCounterDeskDemo() {
  return (
    <div className="w-full">
      <FaqCounterDesk />
    </div>
  );
}
