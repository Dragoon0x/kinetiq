"use client";

import { ContentChapterReel } from "@/registry/blocks/content-chapter-reel/content-chapter-reel";

/** The section at its own scale — full width, default narrative. */
export function ContentChapterReelDemo() {
  return (
    <div className="w-full">
      <ContentChapterReel />
    </div>
  );
}
