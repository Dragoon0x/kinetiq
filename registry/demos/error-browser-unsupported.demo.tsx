"use client";

import { ErrorBrowserUnsupported } from "@/registry/pages/error-browser-unsupported/error-browser-unsupported";

/** The whole page, at its own scale. */
export function ErrorBrowserUnsupportedDemo() {
  return <ErrorBrowserUnsupported />;
}
