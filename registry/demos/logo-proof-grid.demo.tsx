"use client";

import { LogoProofGrid } from "@/registry/blocks/logo-proof-grid/logo-proof-grid";

/** The section at its own scale — full width, default narrative. */
export function LogoProofGridDemo() {
  return (
    <div className="w-full">
      <LogoProofGrid />
    </div>
  );
}
