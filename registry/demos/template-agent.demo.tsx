"use client";

import { TemplateAgent } from "@/registry/templates/template-agent/template-agent";

/** The whole site, at its own scale. */
export function TemplateAgentDemo() {
  return <TemplateAgent />;
}
