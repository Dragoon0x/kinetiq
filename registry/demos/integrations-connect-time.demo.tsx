"use client";

import { IntegrationsConnectTime } from "@/registry/blocks/integrations-connect-time/integrations-connect-time";

/** The section at its own scale — full width, default narrative. */
export function IntegrationsConnectTimeDemo() {
  return (
    <div className="w-full">
      <IntegrationsConnectTime />
    </div>
  );
}
