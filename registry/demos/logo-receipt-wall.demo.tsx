"use client";

import { LogoReceiptWall } from "@/registry/blocks/logo-receipt-wall/logo-receipt-wall";

/** The section at its own scale — full width, default narrative. */
export function LogoReceiptWallDemo() {
  return (
    <div className="w-full">
      <LogoReceiptWall />
    </div>
  );
}
