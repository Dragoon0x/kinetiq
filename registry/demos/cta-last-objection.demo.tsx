"use client";

import { CtaLastObjection } from "@/registry/blocks/cta-last-objection/cta-last-objection";

/** The section at its own scale — full width, default narrative. */
export function CtaLastObjectionDemo() {
  return (
    <div className="w-full">
      <CtaLastObjection />
    </div>
  );
}
