"use client";

import { UsecaseTwoMornings } from "@/registry/blocks/usecase-two-mornings/usecase-two-mornings";

/** The section at its own scale — full width, default narrative. */
export function UsecaseTwoMorningsDemo() {
  return (
    <div className="w-full">
      <UsecaseTwoMornings />
    </div>
  );
}
