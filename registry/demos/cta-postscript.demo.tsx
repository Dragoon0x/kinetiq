"use client";

import { CtaPostscript } from "@/registry/blocks/cta-postscript/cta-postscript";

/** The section at its own scale — full width, default narrative. */
export function CtaPostscriptDemo() {
  return (
    <div className="w-full">
      <CtaPostscript />
    </div>
  );
}
