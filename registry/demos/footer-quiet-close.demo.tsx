"use client";

import { FooterQuietClose } from "@/registry/blocks/footer-quiet-close/footer-quiet-close";

/** The section at its own scale — full width, default narrative. */
export function FooterQuietCloseDemo() {
  return (
    <div className="w-full">
      <FooterQuietClose />
    </div>
  );
}
