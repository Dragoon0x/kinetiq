"use client";

import { IntegrationsTwoWay } from "@/registry/blocks/integrations-two-way/integrations-two-way";

/** The section at its own scale — full width, default narrative. */
export function IntegrationsTwoWayDemo() {
  return (
    <div className="w-full">
      <IntegrationsTwoWay />
    </div>
  );
}
