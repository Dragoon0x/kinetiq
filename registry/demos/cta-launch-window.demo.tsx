"use client";

import { CtaLaunchWindow } from "@/registry/blocks/cta-launch-window/cta-launch-window";

/** The section at its own scale — full width, default narrative. */
export function CtaLaunchWindowDemo() {
  return (
    <div className="w-full">
      <CtaLaunchWindow />
    </div>
  );
}
