"use client";

import { AuthSecondFactor } from "@/registry/pages/auth-second-factor/auth-second-factor";

/** The whole page, at its own scale. */
export function AuthSecondFactorDemo() {
  return <AuthSecondFactor />;
}
