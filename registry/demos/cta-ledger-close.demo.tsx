"use client";

import { CtaLedgerClose } from "@/registry/blocks/cta-ledger-close/cta-ledger-close";

/** The section at its own scale — full width, default narrative. */
export function CtaLedgerCloseDemo() {
  return (
    <div className="w-full">
      <CtaLedgerClose />
    </div>
  );
}
