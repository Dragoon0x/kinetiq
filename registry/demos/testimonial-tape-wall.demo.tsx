"use client";

import { TestimonialTapeWall } from "@/registry/blocks/testimonial-tape-wall/testimonial-tape-wall";

/** The section at its own scale — full width, default narrative. */
export function TestimonialTapeWallDemo() {
  return (
    <div className="w-full">
      <TestimonialTapeWall />
    </div>
  );
}
