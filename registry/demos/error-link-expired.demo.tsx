"use client";

import { ErrorLinkExpired } from "@/registry/pages/error-link-expired/error-link-expired";

/** The whole page, at its own scale. */
export function ErrorLinkExpiredDemo() {
  return <ErrorLinkExpired />;
}
