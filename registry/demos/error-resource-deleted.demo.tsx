"use client";

import { ErrorResourceDeleted } from "@/registry/pages/error-resource-deleted/error-resource-deleted";

/** The whole page, at its own scale. */
export function ErrorResourceDeletedDemo() {
  return <ErrorResourceDeleted />;
}
