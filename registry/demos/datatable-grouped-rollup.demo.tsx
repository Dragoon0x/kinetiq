"use client";

import { DatatableGroupedRollup } from "@/registry/blocks/datatable-grouped-rollup/datatable-grouped-rollup";

/** The section at its own scale — full width, default narrative. */
export function DatatableGroupedRollupDemo() {
  return (
    <div className="w-full">
      <DatatableGroupedRollup />
    </div>
  );
}
