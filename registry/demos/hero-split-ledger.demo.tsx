"use client";

import { HeroSplitLedger } from "@/registry/blocks/hero-split-ledger/hero-split-ledger";

/** The section at its own scale — full width, default narrative. */
export function HeroSplitLedgerDemo() {
  return (
    <div className="w-full">
      <HeroSplitLedger />
    </div>
  );
}
