"use client";

import { AuthWorkspacePick } from "@/registry/pages/auth-workspace-pick/auth-workspace-pick";

/** The whole page, at its own scale. */
export function AuthWorkspacePickDemo() {
  return <AuthWorkspacePick />;
}
