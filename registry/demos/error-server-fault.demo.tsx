"use client";

import { ErrorServerFault } from "@/registry/pages/error-server-fault/error-server-fault";

/** The whole page, at its own scale. */
export function ErrorServerFaultDemo() {
  return <ErrorServerFault />;
}
