"use client";

import { GalleryCoverShelf } from "@/registry/blocks/gallery-cover-shelf/gallery-cover-shelf";

/** The section at its own scale — full width, default narrative. */
export function GalleryCoverShelfDemo() {
  return (
    <div className="w-full">
      <GalleryCoverShelf />
    </div>
  );
}
