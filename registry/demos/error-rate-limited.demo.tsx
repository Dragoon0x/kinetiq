"use client";

import { ErrorRateLimited } from "@/registry/pages/error-rate-limited/error-rate-limited";

/** The whole page, at its own scale. */
export function ErrorRateLimitedDemo() {
  return <ErrorRateLimited />;
}
