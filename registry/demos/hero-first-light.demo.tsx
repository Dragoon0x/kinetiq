"use client";

import { HeroFirstLight } from "@/registry/blocks/hero-first-light/hero-first-light";

/** The section at its own scale — full width, default narrative. */
export function HeroFirstLightDemo() {
  return (
    <div className="w-full">
      <HeroFirstLight />
    </div>
  );
}
