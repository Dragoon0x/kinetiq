"use client";

import { StepformResumeLater } from "@/registry/blocks/stepform-resume-later/stepform-resume-later";

/** The section at its own scale — full width, default narrative. */
export function StepformResumeLaterDemo() {
  return (
    <div className="w-full">
      <StepformResumeLater />
    </div>
  );
}
