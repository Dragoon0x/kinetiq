"use client";

import { TeamWhereWeAre } from "@/registry/blocks/team-where-we-are/team-where-we-are";

/** The section at its own scale — full width, default narrative. */
export function TeamWhereWeAreDemo() {
  return (
    <div className="w-full">
      <TeamWhereWeAre />
    </div>
  );
}
