import type { KinetiqItem } from "./types";

/**
 * Whole page compositions, serials KP-001+. Each one assembles shipped
 * sections and ships with a `target` so the CLI knows where to write it.
 */
export const pages: KinetiqItem[] = [];
